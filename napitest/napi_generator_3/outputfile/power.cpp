#include "power.h"

namespace power {
bool shutdownDevice(std::string &reason) {
    return true;
}

bool rebootDevice(std::string &reason) {
    return true;
}

bool isScreenOn(bool &out) {
    return true;
}
}
