import("//build/ohos.gni")
import("//build/test.gni")
import("//foundation/multimodalinput/input/multimodalinput_mini.gni")
module_output_path = "multimodalinput/systemtest_out"

ohos_systemtest("multi_handout_trackball_reg_B") {
  module_out_path = module_output_path
  include_dirs = [
   "../include",
    "//foundation/multimodalinput/input/interfaces/native/innerkits/event/include",
  ]
  sources = [
    "../common/MultiInputCommon.cpp",
    "../common/auto_test_main.cpp",
    "../common/auto_test_msg_handle.cpp",
    "../common/auto_test_data_process.cpp", 
    "multi_handout_trackball_reg_B.cpp",
  ]

  cflags = [
    "-DOHOS_BUILD",
    "-DOHOS_AUTO_TEST_FRAME",
  ]

  if (is_mmi_have_libinput) {
    cflags += [
      "-DOHOS_BUILD_LIBINPUT",
    ]
  }

  if (is_mmi_have_hdf) {
    cflags += [
      "-DOHOS_BUILD_HDF",
    ]
  }

  if (is_mmi_have_ai) {
    cflags += [
      "-DOHOS_BUILD_AI",
    ]
  }

  deps = [
    "//foundation/multimodalinput/input/frameworks/util:libmmi-util",
    "//foundation/multimodalinput/input/frameworks/proxy:libmmi-client",
    "//foundation/multimodalinput/input/patch/diff_libinput_mmi:libinput-third-mmi",
    "//third_party/googletest:gmock_main",
    "//third_party/googletest:gtest_main"
  ]

  external_deps = [
      "hiviewdfx_hilog_native:libhilog"
  ]
}

ohos_systemtest("multi_handout_trackball_unReg_D") {
  module_out_path = module_output_path
  include_dirs = [
   "../include",
    "//foundation/multimodalinput/input/interfaces/native/innerkits/event/include",
  ]
  sources = [
    "../common/MultiInputCommon.cpp",
    "../common/auto_test_main.cpp",
    "../common/auto_test_msg_handle.cpp",
    "../common/auto_test_data_process.cpp", 
    "multi_handout_trackball_unReg_D.cpp",
  ]

  cflags = [
    "-DOHOS_BUILD",
    "-DOHOS_AUTO_TEST_FRAME",
  ]

  if (is_mmi_have_libinput) {
    cflags += [
      "-DOHOS_BUILD_LIBINPUT",
    ]
  }

  if (is_mmi_have_hdf) {
    cflags += [
      "-DOHOS_BUILD_HDF",
    ]
  }

  if (is_mmi_have_ai) {
    cflags += [
      "-DOHOS_BUILD_AI",
    ]
  }

  deps = [
    "//foundation/multimodalinput/input/frameworks/util:libmmi-util",
    "//foundation/multimodalinput/input/frameworks/proxy:libmmi-client",
    "//foundation/multimodalinput/input/patch/diff_libinput_mmi:libinput-third-mmi",
    "//third_party/googletest:gmock_main",
    "//third_party/googletest:gtest_main"
  ]

  external_deps = [
      "hiviewdfx_hilog_native:libhilog"
  ]
}

ohos_systemtest("multi_handout_trackball_reg_C") {
  module_out_path = module_output_path
  include_dirs = [
   "../include",
    "//foundation/multimodalinput/input/interfaces/native/innerkits/event/include",
  ]
  sources = [
    "../common/MultiInputCommon.cpp",
    "../common/auto_test_main.cpp",
    "../common/auto_test_msg_handle.cpp",
    "../common/auto_test_data_process.cpp", 
    "multi_handout_trackball_reg_C.cpp",
  ]

  cflags = [
    "-DOHOS_BUILD",
    "-DOHOS_AUTO_TEST_FRAME",
  ]

  if (is_mmi_have_libinput) {
    cflags += [
      "-DOHOS_BUILD_LIBINPUT",
    ]
  }

  if (is_mmi_have_hdf) {
    cflags += [
      "-DOHOS_BUILD_HDF",
    ]
  }

  if (is_mmi_have_ai) {
    cflags += [
      "-DOHOS_BUILD_AI",
    ]
  }

  deps = [
    "//foundation/multimodalinput/input/frameworks/util:libmmi-util",
    "//foundation/multimodalinput/input/frameworks/proxy:libmmi-client",
    "//foundation/multimodalinput/input/patch/diff_libinput_mmi:libinput-third-mmi",
    "//third_party/googletest:gmock_main",
    "//third_party/googletest:gtest_main"
  ]

  external_deps = [
      "hiviewdfx_hilog_native:libhilog"
  ]
}

ohos_systemtest("multi_handout_trackball_unReg_R") {
  module_out_path = module_output_path
  include_dirs = [
   "../include",
    "//foundation/multimodalinput/input/interfaces/native/innerkits/event/include",
  ]
  sources = [
    "../common/MultiInputCommon.cpp",
    "../common/auto_test_main.cpp",
    "../common/auto_test_msg_handle.cpp",
    "../common/auto_test_data_process.cpp", 
    "multi_handout_trackball_unReg_R.cpp",
  ]

  cflags = [
    "-DOHOS_BUILD",
    "-DOHOS_AUTO_TEST_FRAME",
  ]

  if (is_mmi_have_libinput) {
    cflags += [
      "-DOHOS_BUILD_LIBINPUT",
    ]
  }

  if (is_mmi_have_hdf) {
    cflags += [
      "-DOHOS_BUILD_HDF",
    ]
  }

  if (is_mmi_have_ai) {
    cflags += [
      "-DOHOS_BUILD_AI",
    ]
  }

  deps = [
    "//foundation/multimodalinput/input/frameworks/util:libmmi-util",
    "//foundation/multimodalinput/input/frameworks/proxy:libmmi-client",
    "//foundation/multimodalinput/input/patch/diff_libinput_mmi:libinput-third-mmi",
    "//third_party/googletest:gmock_main",
    "//third_party/googletest:gtest_main"
  ]

  external_deps = [
      "hiviewdfx_hilog_native:libhilog"
  ]
}

ohos_systemtest("multi_handout_trackball_unReg_H") {
  module_out_path = module_output_path
  include_dirs = [
   "../include",
    "//foundation/multimodalinput/input/interfaces/native/innerkits/event/include",
  ]
  sources = [
    "../common/MultiInputCommon.cpp",
    "../common/auto_test_main.cpp",
    "../common/auto_test_msg_handle.cpp",
    "../common/auto_test_data_process.cpp",
    "multi_handout_trackball_unReg_H.cpp",
  ]

  cflags = [
    "-DOHOS_BUILD",
    "-DOHOS_AUTO_TEST_FRAME",
  ]

  if (is_mmi_have_libinput) {
    cflags += [
      "-DOHOS_BUILD_LIBINPUT",
    ]
  }

  if (is_mmi_have_hdf) {
    cflags += [
      "-DOHOS_BUILD_HDF",
    ]
  }

  if (is_mmi_have_ai) {
    cflags += [
      "-DOHOS_BUILD_AI",
    ]
  }

  deps = [
    "//foundation/multimodalinput/input/frameworks/util:libmmi-util",
    "//foundation/multimodalinput/input/frameworks/proxy:libmmi-client",
    "//foundation/multimodalinput/input/patch/diff_libinput_mmi:libinput-third-mmi",
    "//third_party/googletest:gmock_main",
    "//third_party/googletest:gtest_main"
  ]

  external_deps = [
      "hiviewdfx_hilog_native:libhilog"
  ]
}

group("trackball")
{
    testonly = true
    deps = [":multi_handout_trackball_reg_B",
    ":multi_handout_trackball_unReg_D",
    ":multi_handout_trackball_reg_C",
    ":multi_handout_trackball_unReg_R",
    ":multi_handout_trackball_unReg_H",
    ]
}
